name: ml_example

entry_points:
  main:
    parameters:
        msg1: {default: 'Initial test'}
        http: {default: '{mlparams["http"]}'}
    command: 'python -c "import app.main; app.main.process({http})"'